# 维护约定

这份文件是给「以后接手这个项目的人（或 AI 助手）」看的。
记录的是**这个项目特有的做法**，不是通用最佳实践 —— 每条都对应一次踩过的坑。

---

## 1. 提交：一律走那个双击脚本

改动做完后，**让用户双击 `推送到GitHub.cmd`**，不要让用户手敲 `git push`。

原因：用户不是开发者。这个脚本会自己处理 git 身份、把完整输出写进
`tmp-push-log.txt`，失败时还能从日志里看出原因。

脚本的日志里，**只有最后三节可信**：

```
--- push output (this is the important part) ---
--- push exit code: N ---
--- remote branches after push ---
```

它上面那节 `--- unpushed commits ---` 名字起错了，实际列的是整个历史，
不要拿它判断"有几个提交没推"。判断有没有推上去，看 `push exit code: 0`
和 `remote branches after push` 里那个提交号。

---

## 2. 涉及服务器的操作：把完整命令写出来

**每次让用户去服务器上做事，都要把「怎么连上去」那一步一起写出来**，
不能只给中间那一条命令。

用户的原话是「终端打开服务器的代码每次记得告诉我」。反面例子：

> ❌ 服务器上执行：`cd /opt/study-guardian && git pull`

这样用户会卡在"我怎么进服务器"。正确写法见 `本地部署备忘.md`（那个文件
不入库，里面有真实的 IP 和连接方式），每次把那一整段贴给用户。

---

## 3. 更新服务器的顺序，不能反

```
（电脑）双击 推送到GitHub.cmd  →  （服务器）git pull  →  pm2 restart
```

**反了就静默失败**：先 `pm2 restart` 再 `git pull`，等于"认认真真重启了一个旧版本"，
页面上看不出任何异常，只是新功能不在。这个坑踩过一次。

`git pull` 必须看到 `Fast-forward`。如果它说 `Already up to date.`
而用户刚在电脑上推过，说明推送那一步没成功 —— 不要在电脑上 `git pull`
（那会把服务器上的东西往回拉，曾经因此把一个提交搞丢过）。

---

## 4. 测试：两个都要跑，新守卫要反向验

```bash
npm test          # 单元
npm run test:e2e  # 端到端
npm run test:all  # 提交前跑这个
```

**每加一条断言，就把对应的 bug 真的注射回去，确认它确实变红，再改回来。**
这个项目的断言不止一次是"永远为真"的 —— README 里记了五个真实例子。
写守卫时特别注意这几类：

- 守卫被**自己的注释**骗了 → 匹配前先去注释。
- `indexOf(A) < indexOf(B)` 在两边都找不到时（都是 `-1`）**恒为真**。
- `if (!x) return;` 这种"前置条件不满足就跳过"，会让断言**从来没跑过**。
  只要断言依赖某个东西存在，就先断言那个东西真的存在。
- 用固定长度窗口去 `indexOf` 一段 HTML，会被前面的图标撑爆。
- `.btn--sm {` 是 `.btn--icon.btn--sm {` 的子串 —— 匹配选择器要认边界。

---

## 5. 颜色：改之前先算对比度，别照抄设计稿

`tests/design-tokens.test.js` 会直接从 CSS 解析真实色值算对比度。
两条最容易搞错的：

- **要按这个颜色「能落到的最差底色」算**，不是按白底算。
  浅色模式最差是 `--c-surface-3`；深色模式最差的是**最亮**的那层（也是它）。
  徽标更麻烦：它的底是"状态色 + 10% 透明"，那层底再叠在灰面板上还会更暗。
- **`--c-primary` 和 `--c-primary-solid` 不能合并**。深色模式下前者要够亮
  （当文字看），后者要够深（压白字）。合成一个值，白字就只有 2.7:1。

改完色值记得连带改 `scripts/make-icons.js` 和 `src/web/public/favicon.svg` ——
手机主屏图标不会跟着 CSS 自己变。

---

## 6. 什么不要提交

仓库是**公开**的。根目录下别人给的审计报告 / 设计方案（`/*审计*.md`、
`/*视觉升级方案*.md` 等）已经写进 `.gitignore` —— 它们描述的是某一时刻的
待办清单，代码改完之后不会跟着变，留在仓库里只会变成误导；而且这类文件
经常带着公网 IP 和账号名。

**任何时候都不要把公网 IP、账号名、邀请码、Bark Key、SMTP 授权码写进仓库文件。**
真实的部署信息放在 `本地部署备忘.md`（已 gitignore）。

---

## 7. 干活的方式

- 用户不是开发者：说普通话，命令给到能直接复制，自己错了要直说。
- 一次做完一批再让用户推送 / 更新，不要把用户当成流水线。
- 范围由用户定。用户说"先看看效果"的时候，就不要顺手把结构也改了。
