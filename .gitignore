# 运行时数据：数据库、上传的课件、转换缓存、密钥、日志
# 这些内容包含个人隐私（课表、成绩、账号），永远不要提交
/data/
/logs/

# 本地环境变量（含 SMTP 授权码、Bark Key 等敏感信息）
.env
.env.local

# 测试与临时产物
/tmp/
*.log
npm-debug.log*
# 开发过程中随手写的临时脚本 / 检查文件，形如 tmp-check.mjs、tmp-health.mjs
tmp-*

# 随手丢在项目根目录的截图 / 图片（咨询问题时贴的那种）
# 这些往往带着个人信息（订单、账号、聊天记录），很容易被 `git add -A` 顺手提交上去。
# 只忽略根目录这一层：src/web/public/ 下的图标和 favicon 照常提交。
# 如果确实需要往仓库里放配图，建议放在专门的目录里（比如 docs/）。
/*.png
/*.jpg
/*.jpeg
/*.webp
/*.gif
/*.bmp

# 编辑器
.vscode/
.idea/
*.swp
.DS_Store

# 外部审计 / 测试报告（放在根目录的那种）
# 这类文件几乎一定带着**公网 IP、账号名、测试时间**等信息 ——
# 对「只想自用」的项目来说，公网 IP 本身就是不该公开的东西。
# 和上面截图那一节是同一个道理：很容易被 `git add -A` 顺手带上去。
# 确实想长期保留，就移到 docs/ 里，并先把 IP 之类的信息删掉。
/*审计*.md
/*测试报告*.md
/*测试与优化建议*.md
# 外部给的设计方案 / 重构方案（也是放根目录的那种）
# 这一类是「别人写给我看的输入」，不是仓库内容：它们描述的是**某一时刻**的
# 待办清单，代码改完之后文档不会跟着变，留在仓库里只会慢慢变成误导
# （比如里面还在要求"把三级文字对比度提上去"，而这件事早就做完了）。
# 里面的结论真正落地了，就该写进代码注释和 README，而不是留一份过期清单。
/*视觉升级方案*.md
/*重构方案*.md
/*设计方案*.md
# npm（本项目零依赖，通常不会产生 node_modules；npm 缓存目录也放这里）
node_modules/
